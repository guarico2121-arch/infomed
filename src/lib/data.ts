import type { Doctor } from './types';
import { PlaceHolderImages } from './placeholder-images';

// This file is being deprecated for dynamic data.
// It is kept to avoid breaking imports, but the `doctors` array is now empty.
export const doctors: Doctor[] = [];
